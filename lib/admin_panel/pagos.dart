import 'package:flutter/material.dart';

class PagosPage extends StatelessWidget {
  const PagosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pagos (próximamente)')),
      body: const Center(child: Text('Pantalla de Pagos – MVP en construcción')),
    );
  }
}
